{
  "name": "chordsheetz",
  "private": true,
  "workspaces": [
    "theme/static_src"
  ],
  "scripts": {
    "build": "npm --workspace theme/static_src run build",
    "heroku-postbuild": "npm run build"
  }
}